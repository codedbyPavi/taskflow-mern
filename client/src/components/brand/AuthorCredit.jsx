const AuthorCredit = ({ className = "" }) => (
  <p className={`text-center text-[10px] leading-relaxed tracking-wide text-sidebar-muted/75 ${className}`.trim()}>
    TaskFlow · Built by Pavithra
  </p>
);

export default AuthorCredit;
